import { Row, Select } from "antd";
import styled from "styled-components";

const ContentHeader = styled(Row)`
  margin-bottom: 10px;
`;

const TypeFilter = styled(Select)`
  width: 100%;
`;

export { ContentHeader, TypeFilter };
